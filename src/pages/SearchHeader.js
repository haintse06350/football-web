import React from 'react';
import { Search } from '../components/Search';

export const SearchHeader = class extends React.Component {
  render() {
    return (
      <main>
        <Search />
      </main>
    )
  }
}